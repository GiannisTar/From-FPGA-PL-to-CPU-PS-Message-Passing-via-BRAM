# From-FPGA-PL-to-CPU-PS-Message-Passing-via-BRAM
Zynq PL→PS message passing on a PYNQ-Z2: PL writes into on-chip BRAM, a bare-metal PS app polls and prints it over UART, with an Icarus Verilog testbench suite validating the receiver logic.
